const places = document.getElementsByClassName('importent-places');
for(const place of places){
    // console.log(place.innerText)
}

const heading = document.getElementById('heading');
heading.innerText = 'place change By JavaScript';


// querySlector

const samoli = document.querySelectorAll('.placeess li');
// console.log(samoli);
for(const samol of samoli){
    // console.log(samol);
}